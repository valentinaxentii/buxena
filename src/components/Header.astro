---
import { nav, site } from '../data/site';

interface Props { overHero?: boolean; }
const { overHero = false } = Astro.props;

const path = Astro.url.pathname;
const isActive = (href: string) => path === href || (href !== '/' && path.startsWith(href));
---

<header class={`hdr ${overHero ? 'hdr--over' : ''}`} data-header>
  <div class="hdr__inner">
    <a class="wordmark" href="/" aria-label={`${site.name} home`}>
      <img
        class="wordmark__logo wordmark__logo--dark"
        src="/brand/buxena-logo-transparent.png"
        alt=""
        width="700"
        height="379"
      />
      <img
        class="wordmark__logo wordmark__logo--light"
        src="/brand/buxena-logo-champagne.png"
        alt=""
        width="1536"
        height="1024"
      />
    </a>

    <nav class="hdr__nav" aria-label="Primary">
      <ul>
        {nav.map((item) => (
          <li>
            <a href={item.href} aria-current={isActive(item.href) ? 'page' : undefined}>
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="hdr__end">
      <a class="btn btn--bronze hdr__cta" href="/quote/">Request a Quote</a>
      <button class="burger" type="button" aria-expanded="false" aria-controls="mobile-nav" data-burger>
        <span class="sr-only">Open menu</span>
        <span class="burger__bar" aria-hidden="true"></span>
        <span class="burger__bar" aria-hidden="true"></span>
      </button>
    </div>
  </div>
</header>

<div class="drawer" id="mobile-nav" hidden data-drawer>
  <nav aria-label="Mobile">
    <ul>
      {nav.map((item) => (
        <li><a href={item.href}>{item.label}</a></li>
      ))}
    </ul>
  </nav>
  <a class="btn btn--bronze drawer__cta" href="/quote/">Request a Quote</a>
</div>

<style>
  .hdr {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--linen);
    border-bottom: 1px solid var(--line);
    transition: background-color 0.5s var(--ease), border-color 0.5s var(--ease),
      color 0.5s var(--ease);
  }

  /* transparent over a dark hero, until scrolled */
  .hdr--over {
    position: fixed;
    inset-inline: 0;
    background: transparent;
    border-bottom-color: transparent;
    color: var(--linen);
  }
  .hdr--over.is-stuck {
    background: var(--linen);
    border-bottom-color: var(--line);
    color: var(--ink);
    /* Snap on rather than fade. The transition below is declared on .hdr, so
       it applies in both directions; while the background faded in over half
       a second the page content scrolled visibly through the header. Zeroing
       the duration here only affects the moment the class is ADDED — removing
       it (scrolling back to the hero) still uses the base transition. */
    transition-duration: 0s;
  }

  .hdr__inner {
    max-width: var(--shell);
    margin-inline: auto;
    padding: 0 var(--gutter);
    height: clamp(7.5rem, 7.25rem + 3.15vw, 10.75rem);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .wordmark {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    white-space: nowrap;
  }

  /* new lockup is stacked (steam / wordmark / line / tagline) rather than a
     flat wide mark, so it is sized by width — height follows the intrinsic
     1536x1024 ratio automatically. ~145-165px mobile, ~170-190px tablet,
     ~190-220px desktop. */
  .wordmark__logo {
    display: block;
    width: clamp(9.0625rem, 8.6rem + 4.7vw, 13.75rem);
    height: auto;
  }

  /* dark (colored) logo by default — solid header on light backgrounds */
  .wordmark__logo--light { display: none; }

  /* light logo while transparent over the hero video; swap to dark once stuck/solid */
  .hdr--over:not(.is-stuck) .wordmark__logo--dark { display: none; }
  .hdr--over:not(.is-stuck) .wordmark__logo--light { display: block; }

  .hdr__nav ul {
    display: flex;
    gap: clamp(1.4rem, 0.6rem + 1.9vw, 2.75rem);
  }

  .hdr__nav a {
    font-size: 0.9375rem;
    font-weight: 300;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    text-decoration: none;
    padding-block: 0.4rem;
    border-bottom: 1px solid transparent;
    transition: border-color 0.35s var(--ease), opacity 0.35s var(--ease);
    opacity: 0.92;
  }
  .hdr__nav a:hover { opacity: 1; border-bottom-color: currentColor; }
  .hdr__nav a[aria-current='page'] { opacity: 1; border-bottom-color: var(--bronze); }

  .hdr__end { display: flex; align-items: center; gap: 1rem; }
  .hdr__cta { padding: 0.85rem 1.5rem; }

  .burger {
    display: none;
    background: none;
    border: none;
    cursor: pointer;
    /* 44×44 hit area — this is the only way to reach navigation on a phone,
       and at the old 34×27 it was smaller than a fingertip. The bars keep
       their drawn width; the rest is invisible padding. */
    width: 44px;
    height: 44px;
    padding: 0;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    /* Pull the button back to the edge so the larger hit area does not
       visually indent the icon from the header's right margin. */
    margin-right: -0.5rem;
  }
  .burger__bar {
    display: block;
    width: 2.1rem;
    height: 1px;
    background: currentColor;
    transition: transform 0.4s var(--ease), opacity 0.3s var(--ease);
  }
  .burger__bar + .burger__bar { margin-top: 6px; }
  .burger[aria-expanded='true'] .burger__bar:first-child { transform: translateY(3.5px) rotate(45deg); }
  .burger[aria-expanded='true'] .burger__bar:last-child { transform: translateY(-3.5px) rotate(-45deg); }

  /* --- drawer --- */
  .drawer {
    position: fixed;
    inset: clamp(7.5rem, 7.25rem + 3.15vw, 10.75rem) 0 auto 0;
    z-index: 99;
    background: var(--linen);
    border-bottom: 1px solid var(--line);
    padding: 1.5rem var(--gutter) 2.5rem;
    display: grid;
    gap: 1.5rem;
  }
  .drawer[hidden] { display: none; }
  .drawer ul { display: grid; }
  .drawer a {
    display: block;
    font-family: var(--font-display);
    font-size: 1.6rem;
    text-decoration: none;
    padding: 0.7rem 0;
    border-bottom: 1px solid var(--line);
    color: var(--ink);
  }
  .drawer__cta { justify-content: center; color: #fff; border-bottom: none; font-family: var(--font-sans); font-size: var(--t-label); }

  @media (max-width: 1000px) {
    .hdr__nav { display: none; }
    /* flex, not block — the button centres its bars in the 44px hit area */
    .burger { display: flex; }
    .hdr__cta { display: none; }
  }
</style>

<script>
  const header = document.querySelector<HTMLElement>('[data-header]');
  const burger = document.querySelector<HTMLButtonElement>('[data-burger]');
  const drawer = document.querySelector<HTMLElement>('[data-drawer]');

  if (header?.classList.contains('hdr--over')) {
    const onScroll = () => header.classList.toggle('is-stuck', window.scrollY > 40);
    onScroll();
    window.addEventListener('scroll', onScroll, { passive: true });
  }

  const setOpen = (open: boolean) => {
    if (!burger || !drawer) return;
    burger.setAttribute('aria-expanded', String(open));
    drawer.hidden = !open;
    burger.querySelector('.sr-only')!.textContent = open ? 'Close menu' : 'Open menu';

    // On the homepage the header sits transparent over a dark hero with light
    // text. The drawer is cream, so force the solid state while it is open or
    // the burger and wordmark disappear against it.
    if (header?.classList.contains('hdr--over')) {
      if (open) header.classList.add('is-stuck');
      else header.classList.toggle('is-stuck', window.scrollY > 40);
    }

    document.body.style.overflow = open ? 'hidden' : '';
  };

  burger?.addEventListener('click', () => {
    setOpen(burger.getAttribute('aria-expanded') !== 'true');
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && burger?.getAttribute('aria-expanded') === 'true') {
      setOpen(false);
      burger.focus();
    }
  });

  // close the drawer if the viewport grows past the breakpoint
  window.matchMedia('(min-width: 1001px)').addEventListener('change', (e) => {
    if (e.matches) setOpen(false);
  });
</script>
