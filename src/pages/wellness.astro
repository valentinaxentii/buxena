---
import BaseLayout from '../layouts/BaseLayout.astro';
import Figure from '../components/Figure.astro';
import QuoteBand from '../components/QuoteBand.astro';

const rounds = [
  {
    n: '01',
    title: 'Heat',
    body: 'Sit for as long as it stays comfortable and no longer. Ten to fifteen minutes is a common first round. The upper bench is hotter than the lower one — start low.',
  },
  {
    n: '02',
    title: 'Löyly',
    body: 'A ladle of water on the stones lifts the humidity sharply and the room feels hotter within seconds. Add it gradually. This is the part people build the ritual around.',
  },
  {
    n: '03',
    title: 'Cool',
    body: 'Leave before you want to. Step outside, take a cool shower, sit down. The contrast is the point, and rushing it is the most common mistake.',
  },
  {
    n: '04',
    title: 'Rest',
    body: 'Wait until your breathing has settled and you feel genuinely cool again. Then decide whether to go back in. Two or three rounds is typical.',
  },
];
---

<BaseLayout
  title="Wellness"
  description="How to use a sauna well — the heat, cold and rest cycle, etiquette, and practical guidance for getting the most from the room."
>
  <section class="band band--tight page-head">
    <div class="shell">
      <span class="eyebrow">The Ritual</span>
      <h1 class="display measure--tight">Heat, cold, rest — and then again</h1>
      <p class="lead measure">
        A sauna rewards patience more than endurance. The rhythm below is the one
        used across northern Europe, and it is worth learning before you buy.
      </p>

      <div class="ritual-photo reveal">
        {/* IMAGE RIGHTS 2026-08-13 — RESOLVED. Founder-provided photograph
            (supplied 2026-08-13, extracted from the founder's one-page PDF).
            Registered in docs/image-rights-register.md as FOUNDER-PROVIDED —
            APPROVED.

            Crop: the source is 2048×1082 (1.89) against a 16/9 frame (1.78),
            so `cover` trims only ~6% horizontally — 3% from each edge. Every
            element the founder asked to keep survives at every width: bench
            left, bucket, stone heater centre, glass wall and the lounge chair
            beyond it right. Default centre focus is correct here; biasing it
            either way would push one of those out of frame. */}
        <Figure
          src="/images/editorial/sauna-glass-lounge.jpg"
          alt="Tiered sauna benches and a black bucket on the left, a tall stone-filled heater at the centre, and a full-height glass wall on the right looking through to a lounge chair and trees beyond"
          ratio="16 / 9"
          tone="dark"
          priority={true}
        />
      </div>
    </div>
  </section>

  <section class="band ritual-steps">
    <div class="shell">
      <ol class="rounds reveal">
        {rounds.map((r) => (
          <li class="round">
            <span class="round__n">{r.n}</span>
            <div>
              <h2 class="h3 round__title">{r.title}</h2>
              <p class="round__body">{r.body}</p>
            </div>
          </li>
        ))}
      </ol>
    </div>
  </section>

  <section class="band band--birch etiquette-section">
    <div class="shell split">
      <div class="split__media reveal">
        {/* IMAGE RIGHTS 2026-08-13 — RESOLVED. Founder-provided photograph
            (supplied 2026-08-13, extracted losslessly from the founder's
            one-page PDF). Registered in docs/image-rights-register.md as
            FOUNDER-PROVIDED — APPROVED.

            Crop: source is 1024×1461 (0.70) in a 4/5 frame (0.80), so `cover`
            trims ~12% of the height. Centred, that would clip the folded towel
            at the bottom edge; `center 65%` takes the trim almost entirely from
            the plain upper wall instead, so heater, bucket, ladle, towel and
            bench all stay in frame at every width. Ratio unchanged, so the
            section is exactly as tall as it was under the compression pass. */}
        <Figure
          src="/images/editorial/sauna-corner-bench-heater.jpg"
          alt="Sauna interior with wooden benches, stone heater, bucket and ladle"
          ratio="4 / 5"
          tone="dark"
          focus="center 65%"
        />
      </div>
      <div class="split__text reveal">
        <span class="eyebrow">Etiquette</span>
        <h2 class="h2">A few conventions worth keeping</h2>
        <p>
          Sit on a towel. Shower before you go in. Keep the door closed and open it
          decisively rather than slowly — a slow door loses more heat than a quick one.
        </p>
        <p>
          Ask before adding water if you are not alone; humidity is a shared
          decision. Keep conversation quiet or absent. Take off jewellery and
          anything metal.
        </p>
        <p>
          Drink water between rounds, and leave the room whenever it stops being
          pleasant. Nothing about a sauna is improved by pushing through.
        </p>
      </div>
    </div>
  </section>

  <section class="band">
    <div class="shell shell--narrow">
      <div class="disclaimer">
        <span class="eyebrow">Please note</span>
        <p>
          BUXENA makes no medical or therapeutic claims about sauna use. Heat
          exposure is not appropriate for everyone — including during pregnancy,
          with cardiovascular conditions, with certain medications, or in
          combination with alcohol.
        </p>
        <p>
          If you have any health condition, or you are unsure whether sauna use is
          suitable for you, speak to a physician before starting. This page is
          general guidance on how the room is traditionally used, nothing more.
        </p>
      </div>
    </div>
  </section>

  <QuoteBand />
</BaseLayout>

<style>
  .page-head { padding-bottom: 0; }
  .page-head .lead { margin-top: 1.11rem; }

  /* Page-scoped, not the shared --band token: Astro auto-scopes this style
     block to elements wellness.astro itself renders, so `.page-head` and
     `.ritual-steps` here cannot leak into any other page's identically-named
     sections. Needed because the reduction required (outer padding on just
     these two sections cut roughly in half) is specific to fitting THIS
     composition in one screen, not something every --band section should do. */
  .page-head { padding-block: clamp(0.5rem, 0.35rem + 0.5vw, 0.8rem); }
  .ritual-steps { padding-block: clamp(0.35rem, 0.25rem + 0.3vw, 0.55rem); }

  /* ONE-SCREEN RITUAL, 2026-08-14 — founder target: the whole Ritual
     composition (heading, intro, image, all four steps) visible at 1440x900
     with no scroll. Measured baseline before this pass: 1441px of content
     under a 75px header, against 900px available — needed roughly 43% off
     this section specifically, well beyond what the page-wide --band token
     could give without changing height everywhere else, so these are
     page-scoped overrides local to Wellness alone. Mobile is deliberately
     NOT touched here (no media-query override) — it keeps the site-wide
     token values and stacks/scrolls normally, per the founder's explicit
     "mobile can stack naturally" instruction. */
  .ritual-photo {
    max-width: 520px;
    margin-inline: auto;
    margin-top: clamp(0.4rem, 0.2rem + 0.4vw, 0.7rem);
    margin-bottom: clamp(0.4rem, 0.2rem + 0.4vw, 0.7rem);
  }

  .rounds {
    display: grid;
    gap: 0;
  }
  .round {
    display: grid;
    grid-template-columns: minmax(0, 4rem) minmax(0, 1fr);
    gap: clamp(0.8rem, 0.35rem + 1.6vw, 2.1rem);
    padding-block: clamp(0.2rem, 0.15rem + 0.2vw, 0.3rem);
    border-top: 1px solid var(--line);
    align-items: center;
  }
  .round:last-child { border-bottom: 1px solid var(--line); }

  .round__n {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 1.1rem + 1.4vw, 2.2rem);
    line-height: 1;
    color: var(--bronze);
  }
  /* Line-height only (not font-size, which the brief protects): 1.5 → 1.32.
     This is the one real lever left once every pure-whitespace gap on this
     page was already cut — four rows of title+body text is most of what
     makes this section tall, and a leading trim reads as denser typesetting,
     not smaller type. */
  .round__title { margin-bottom: 0.25rem; line-height: 1.25; }
  .round__body { line-height: 1.32; }
  .round__body { color: var(--muted); max-width: 42em; }

  .split {
    display: grid;
    grid-template-columns: minmax(0, 0.85fr) minmax(0, 1fr);
    /* Trimmed ~25% off the max: clamp(1.5rem,...,4.5rem) → ...,3.4rem. */
    gap: clamp(1.3rem, 0.4rem + 3vw, 3.4rem);
    align-items: center;
  }
  .split__text .h2 { margin-bottom: 1.11rem; }

  /* Page-scoped (Astro auto-scopes this style block to wellness.astro's own
     elements — see the identical note on .page-head/.ritual-steps above),
     for the founder's explicit numeric targets on this one section:
     image ~25-30% narrower, section padding ~35-40% tighter. Neither number
     maps to any other page, so this isn't the shared token doing the work —
     it's a deliberate, additional constraint on top of it. */
  .etiquette-section { padding-block: clamp(1.4rem, 0.9rem + 1.2vw, 2.1rem); }
  .split__media { max-width: 330px; }

  .disclaimer {
    border-top: 2px solid var(--ink);
    padding-top: 1.27rem;
  }
  .disclaimer p { font-size: var(--t-small); color: var(--muted); line-height: 1.8; }

  @media (max-width: 900px) {
    .split { grid-template-columns: 1fr; }
    .round { grid-template-columns: 1fr; gap: 0.75rem; }
    /* The 330px desktop cap is an editorial choice for a wide two-column
       layout; once .split stacks to one column on a phone it would just
       leave the portrait photo floating narrow in the middle of the page.
       Let it use the full column width instead, per the founder's own
       "mobile should use most of the available width" direction on this
       page's other image. */
    .split__media { max-width: none; }
  }
</style>
