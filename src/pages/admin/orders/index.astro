---
export const prerender = false;
import AdminLayout from '../../../layouts/AdminLayout.astro';
import '../../../styles/admin.css';
---
<AdminLayout title="Orders">
  <div class="admin-placeholder">
    <h2>Coming in Phase 2</h2>
    <p>
      The <code>orders</code> table already exists — full workflow (Deposit Received → Ordered →
      In Production → Shipped → At Port → Warehouse → Delivery Scheduled → Delivered → Installed)
      plus landed cost / gross profit / gross margin, computed automatically. This screen isn't
      built yet.
    </p>
  </div>
</AdminLayout>
