---
export const prerender = false;
import AdminLayout from '../../../layouts/AdminLayout.astro';
import '../../../styles/admin.css';
---
<AdminLayout title="Inventory">
  <div class="admin-placeholder">
    <h2>Coming in Phase 2</h2>
    <p>
      The <code>inventory</code> table already exists (model, in stock, incoming, reserved,
      available, supplier, container, PO, ETA, port, warehouse, landed cost) — this screen just
      isn't built yet.
    </p>
  </div>
</AdminLayout>
