---
export const prerender = false;
import AdminLayout from '../../../layouts/AdminLayout.astro';
import '../../../styles/admin.css';
---
<AdminLayout title="Analytics">
  <div class="admin-placeholder">
    <h2>Coming in Phase 2</h2>
    <p>
      Leads by source, leads by state, quote conversion, sales pipeline, most-requested models,
      revenue and gross margin — all queryable from tables that already exist. The Dashboard
      already shows a live pipeline breakdown; this page will go deeper.
    </p>
  </div>
</AdminLayout>
