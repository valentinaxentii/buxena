---
export const prerender = false;
import AdminLayout from '../../../layouts/AdminLayout.astro';
import '../../../styles/admin.css';
---
<AdminLayout title="Settings">
  <div class="admin-placeholder">
    <h2>Coming later</h2>
    <p>
      Company information, staff users, quote defaults, tax settings, delivery defaults, email
      settings and lead sources will live here. For now, staff accounts are managed directly in
      the Supabase dashboard (Authentication → Users) — see <code>README-ADMIN.md</code>.
    </p>
  </div>
</AdminLayout>
