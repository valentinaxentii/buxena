---
export const prerender = false;
import AdminLayout from '../../../layouts/AdminLayout.astro';
import '../../../styles/admin.css';
---
<AdminLayout title="Documents">
  <div class="admin-placeholder">
    <h2>Coming in Phase 2</h2>
    <p>
      The <code>documents</code> table exists (category, file reference, and optional links to a
      supplier, customer, product, quote or order) but file upload and this browsing screen
      aren't built yet. Uploading needs Supabase Storage wired up first.
    </p>
  </div>
</AdminLayout>
